import React from "react";

const Contacts = () => {
  return (
    <div className="fixed flex flex-col justify-between px-[64px] py-[8px]">
      <div className="mx-[10px] my-[32px] text-[24px]">Contacts</div>
    </div>
  );
};

export default Contacts;
